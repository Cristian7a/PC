import { Router } from 'express';

const router = Router();

// router.get('/map/reports', getReportsForMapController);
// router.post('/reports', addReportController);
// router.get('/reports/:id', getReportByIdController);

export { router as anonymousRouter };
